update assignment --> v0.3
update assignment --> v0.5
    แก้ path เป็น locator และเปลี่ยนชื่อ โฟลเดอร์ commom_locator เป็น header_menu_locator
    แก้ชื่อ test case
    เพิ่ม reference ให้กับทุก  keyword
    เว้นบรรทัดแต่ละ keyword ให้อ่านง่ายขึ้น
    เพิ่มไฟล์ header_menu_locator และ header_menu_page
    ลบไฟล์ uat  sit ที่ไม่ได้ใช้งาน
    แยก 1 action = 1 keyword ที่พี่รีน่าคอมเมนต์
    แยก ส่วนของ level feature ที่อยู่ใน page ไปไว้ในส่วน feature